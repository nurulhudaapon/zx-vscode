pub fn Page(allocator: zx.Allocator) zx.Component {
    const fruits = [_][]const u8{"Apple", "Banana", "Cherry"};
    return (
        <ul @allocator={allocator}>
            {for (fruits) |f| { (<li>{f}</li>) }}
        </ul>
    );
}

const zx = @import("zx");
